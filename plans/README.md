# Implementation Plans

Generated by the improve skill on 2026-07-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Reframe pi-speak as a conversational assistant hub | P1 | M | — | DONE |

## Dependency notes

- 001 has no dependencies.
- 001 execution notes (2026-07-14):
  - Read-only agent-hub/workspace tools and the mutating-command approval flow needed to reach into `control-server.ts` (exported `listWorkspaceDirectory`/`readWorkspaceFile`, made `agentHubGateway` public) and `realtime-types.ts` (added `command_approve`/`command_reject` control message types) in addition to the plan's listed files — `agent-hub-dashboard.ts` and `herdr-agent-hub-gateway.ts` needed no changes since their existing read-only surface was already sufficient.
  - Implemented the approval boundary as per-tool confirmation-gating (same shape as the existing `execute_terminal_command` flow: call the tool, get `tool_approval_required` back if it mutates, client sends `command_approve`/`command_reject`) via a new `realtime-command-approval.ts` registry, rather than a single generic `propose_command` tool — this matches the existing terminal-approval convention in this codebase instead of introducing a second approval shape.
  - `npm run typecheck` and `npm test` both run clean for the new/changed code; two pre-existing failures are unrelated to this change (`tests/python-health.test.mjs` needs a local Python/faster-whisper install not present in this sandbox; `tests/voice-approval.test.mjs` has a pre-existing flaky timer test) — reproduced against a clean checkout of this branch before these changes.

## Findings considered and rejected

- (none yet)
