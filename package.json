{
  "name": "pi-speak-pk",
  "version": "0.2.7",
  "type": "module",
  "description": "Voice, wake-word, Telegram, and mobile web remote extensions for Pi.",
  "keywords": [
    "pi",
    "pi-coding-agent",
    "extension",
    "speech",
    "text-to-speech",
    "voice",
    "telegram",
    "remote-control",
    "pwa"
  ],
  "main": "dist/index.js",
  "bin": {
    "pi-speak-admin": "dist/ui/admin.js",
    "pi-speak-tray": "dist/persistent-tray.js",
    "pi-speak-gateway": "dist/headless-gateway.js",
    "pi-speak-gemini-live-smoke": "dist/gemini-live-smoke.js",
    "pi-speak-qr": "scripts/qr-setup.mjs"
  },
  "files": [
    "dist",
    "web",
    "assets",
    "android-app/.build-outputs/app-debug.apk",
    "listener",
    "!listener/__pycache__",
    "!listener/**/*.pyc",
    "unified-remote",
    "!dist/**/*.tsbuildinfo",
    "SKILL.md",
    "CHANGELOG.md",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:ui": "tsc -p tsconfig.ui.json && node scripts/sync-ui-package.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.ui.json --noEmit",
    "test": "npm run build && npm run build:ui && node --test tests/*.test.mjs",
    "test:parallel": "npm run build && npm run build:ui && node --test --test-concurrency=4 tests/*.test.mjs",
    "test:coverage": "npm run build && npm run build:ui && c8 --reporter=text --reporter=lcov --include='dist/**/*.js' node --test tests/*.test.mjs",
    "test:stress": "npm run build && npm run build:ui && node --test tests/stress-adversarial.test.mjs",
    "prepublishOnly": "npm run build && npm run build:ui",
    "qr": "node scripts/qr-setup.mjs"
  },
  "dependencies": {
    "@google/genai": "^1.52.0",
    "bonjour-service": "^1.4.0",
    "ink": "^7.0.0",
    "node-edge-tts": "^1.2.10",
    "qrcode": "^1.5.4",
    "react": "^19.2.5"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.66.1",
    "@types/node": "^22.15.3",
    "@types/qrcode": "^1.5.6",
    "@types/react": "^19.2.14",
    "c8": "^10.1.3",
    "typescript": "^5.8.3"
  }
}
