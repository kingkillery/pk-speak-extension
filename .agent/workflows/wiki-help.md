Read `AGENTS.md`, `LLM_WIKI_MEMORY.md` if present, `.llm-wiki/config.json`, and `wiki/index.md` if present.

Use this workflow when the user asks for help with the packet, wiki, memory, skills, install/setup, Obsidian, MCP, repo maps, or commands.

Answer in plain language. Natural-language help requests are enough; do not require the user to know slash commands or internal script names.

If the user asks generally what to do next, give:
- the one-command install path for their shell
- how to verify install health
- how to ask ordinary questions that retrieve source-backed context
- how to save durable answers
- how to generate wiki maps
- how to create reusable skills

If the user asks a specific question, answer that specific question and include only the command or file path needed for the next step.
